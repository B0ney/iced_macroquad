use macroquad::miniquad::{Context, Pipeline};

pub struct State {}

impl State {}

impl State {
    pub fn new(ctx: &mut Context) -> Self {
        Self {}
    }

    pub fn draw(ctx: &mut Context) {}
}
